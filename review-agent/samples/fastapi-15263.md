# Sample Review: claude-builders-bounty#2718

Command:

```bash
./claude-review --pr https://github.com/claude-builders-bounty/claude-builders-bounty/pull/2718
```

Output:

```markdown
## Claude Review

### Summary
This PR changes 4 file(s) in `claude-builders-bounty/claude-builders-bounty`, with 285 added line(s), 0 removed line(s), and 4 diff hunk(s). The main touched paths are: hooks/README.md, hooks/destructive_bash_guard.py, hooks/install.sh, hooks/test_destructive_bash_guard.py. The diff appears to include test coverage.

### Identified Risks
- No high-risk pattern is obvious from the diff statistics; still review domain behavior manually.

### Improvement Suggestions
- Verify the documentation matches current commands and setup paths by following it once locally.
- Review each changed file for ownership boundaries so unrelated cleanup does not hide behavior changes.
- Confirm the PR description states user impact, validation performed, and any known limitations.

### Confidence Score: High

_Generated by `claude-review` from the public GitHub diff._
```
